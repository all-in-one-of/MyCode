# coding=utf-8
import json
import pprint
import ast

class TestIntmeCommodity(object):
    def commodityModel(self, info):
        """
        模型的制作模式
        :param info:json文件
        :return:
        """
        if info[u'制作类型']:
            print info[u'制作类型']
        else:
            print '待定'

    def modelMaker(self, info, name=None):

        if info['制作人']:
            print info['制作人']
        elif name is None:
            print '等待制作'
        else:
            info['制作人'] = name
            with open(r'C:\Users\HYC\Documents\MyCode\maya\intime\a002.json', 'w') as f:
                json.dump(info, f, indent=4, ensure_ascii=False)

    def
if __name__ == '__main__':
    with open(r'C:\Users\HYC\Documents\MyCode\maya\intime\a001.json', 'r') as f:
        info = json.load(f)
        b = json.dumps(info,encoding="utf-8", ensure_ascii=False)
        c = b.encode('utf-8')
        d = eval(c)

    a = TestIntmeCommodity()
    a.commodityModel(info)
    a.modelMaker(d)